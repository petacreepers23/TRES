
#include "print.h"


int main() {
	print();
	/*const char* string = "hola hola";
	const char colour = 15;
	volatile char* mem_video = (volatile char*)0xB8000;

	while (*string != 0) {
		*mem_video++ = *string++;
		*mem_video++ = colour;
	}
	while(1);
	*/
}